const sen="";
const regEx="";
const getFrequency = (pattern, sentence) => {
  try{
return  sentense.match(pattern).length;
  
}catch{
  console.log("Invalid Input")
}
  
  }
console.log(regEx,sen);

const str1 = "I love JS";
console.log(/^Python/ig.test(str1))

const str2 = "I love JS 1";
console.log(/[a-z][^0-9]/ig.test(str2))
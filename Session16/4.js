const names = ["John", "Sherlock", "Mycroft"]
const firstLetters = names.map((name)=>name[0]);
console.log(firstLetters);

const odd=(x)=> x%2!=0;
const oddValues = values.filter((item) => odd(item))
console.log(oddValues)